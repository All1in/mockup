/**
 * Який коміт зараз у проді.
 *
 * Потрібно не людям, а деплою: CI запускає збірку на Vercel через deploy hook
 * і не має іншого способу дізнатись, що доїхала САМЕ його зміна. Стара версія
 * усе життя збірки відповідає 200, тож перевірка «сайт відповідає» зеленіла б
 * миттєво й підтверджувала б попередній деплой.
 *
 * VERCEL_GIT_COMMIT_SHA підставляє сама платформа. Локально його немає — це
 * нормально, тут він нікому не потрібен.
 */
export const dynamic = 'force-dynamic';

export function GET() {
  return Response.json({
    commit: process.env.VERCEL_GIT_COMMIT_SHA ?? 'unknown',
  });
}
